import { ElementType } from 'react'

import { BoxProps } from '@mui/material'

export interface EmptyContentPropTypes {
  title?: string
  description?: string
  icon?: ElementType
  boxProps?: BoxProps
}
