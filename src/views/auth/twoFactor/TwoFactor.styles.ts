import { Stack } from '@mui/material'
import styled from 'styled-components'

export const InputStackStyles = styled(Stack)`
  .MuiInputBase-root {
    margin: 0;
    height: 4rem;
    input {
      padding-left: 0.5rem;
      text-align: center;
    }
  }
`
